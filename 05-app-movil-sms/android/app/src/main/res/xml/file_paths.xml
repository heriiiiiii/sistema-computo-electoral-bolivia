<?xml version="1.0" encoding="utf-8"?>
<!-- Rutas compartidas con la cámara via FileProvider (requerido por image_picker) -->
<paths>
    <external-path name="external_files" path="."/>
    <cache-path name="cache" path="."/>
    <files-path name="files" path="."/>
</paths>
